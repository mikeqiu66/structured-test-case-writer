---
name: structured-test-case-writer
description: |
  用于基于需求文档、PRD、交互说明、版本说明等资料，按固定流程输出结构化测试分析与正式测试用例。适用于需要先做需求拆解、测试点梳理、问题澄清项，再编写测试用例，并可落地到飞书表格/飞书文档的场景。特别适合有版本兼容、状态切换、时间窗口、并发逻辑、异常恢复等复杂规则的项目。输出时应遵循统一测试用例格式：固定字段、操作步骤与预期结果一一对应、统一使用 # 编号、空对应项保留 #。当用户说“按之前流程写测试用例”“按标准测试用例表输出”“先拆需求再写用例”“补测试点/澄清项/覆盖矩阵”时，优先使用本 skill。
---

# Structured Test Case Writer

## 适用时机
当用户提出以下类型需求时使用本 skill：
- “根据需求文档/PRD写测试用例”
- “先拆需求，再整理测试点，再写用例”
- “把测试用例写进飞书表格/文档”
- “补边界/异常/兼容/稳定性测试用例”
- “按固定格式输出正式测试用例表”
- “按之前流程写测试用例”
- “按标准测试用例表输出”
- “先出测试点，再写用例”
- “补问题澄清项 / 覆盖矩阵”

如果任务本质是测试设计，不要直接跳到正式用例；先按本 skill 的分析流程执行。

## 核心原则
1. **先理解资料，再写用例**：禁止在未读需求文档/PRD前直接生成用例。
2. **先分析，后落表**：默认顺序是“需求拆解 → 测试点梳理 → 问题澄清项 → 覆盖矩阵（如需要）→ 正式测试用例 → 落地校验”。
3. **先收敛范围，再扩展深度**：先确认本轮需求范围，再补边界/异常/并发/兼容/稳定性。
4. **内容正确不等于交付完成**：如果目标是飞书文档/表格，必须以“成功写入 + 回读校验”为完成标准。
5. **格式是硬约束**：正式测试用例必须满足本 skill 定义的格式规范。

## 参考模板
如需快速复用模板，按需读取：
- `references/test-case-table-template.md`：标准测试用例表模板
- `references/coverage-matrix-template.md`：覆盖矩阵模板
- `references/clarification-template.md`：问题澄清项模板

## 标准执行流程

### Step 1：收集并读取输入资料
优先识别并读取：
- 需求文档
- PRD / 交互文档
- API / 状态流 / 按键说明 / 调度逻辑
- 版本说明 / 兼容说明
- 历史测试资产（已有用例、缺陷、复盘）

先确认：
- 本轮纳入哪些模块
- 哪些模块不在本轮范围
- 是否已有固定输出格式或表头要求
- 是否要求落地到飞书表格/飞书文档

输出建议：
- 资料来源清单
- 测试范围清单

### Step 2：先做需求拆解
至少拆出：
- 功能目标
- 页面/流程/状态切换逻辑
- 关键规则
- 版本兼容点
- 高风险点
- 时间窗口/调度逻辑（如果有）

### Step 3：输出测试点梳理
按模块梳理，至少覆盖：
- 功能测试点
- 边界测试点
- 异常测试点
- 并发/状态切换测试点
- 兼容测试点
- 稳定性测试点

### Step 4：输出问题澄清项
如果需求存在口径不清、默认态不清、失败回退逻辑不清、兼容范围不清，必须先输出问题澄清项。

澄清项优先关注：
- 默认态
- 回退逻辑
- 状态保留逻辑
- 并发冲突处理规则
- 版本兼容口径
- 优先级规则
- 时间窗口逻辑

### Step 5：必要时补覆盖矩阵
对于存在多个维度组合的项目，先补矩阵，不要直接堆用例。

常见矩阵维度：
- 版本矩阵（旧版/新版）
- 状态矩阵（页面态/设备态/模式态）
- 数据矩阵（空数据/正常数据/边界数据）
- 网络矩阵（正常/弱网/断网）
- 时间矩阵（普通时段/特定窗口/跨天）

### Step 6：正式编写测试用例
默认按以下顺序写：
1. 主流程
2. 边界
3. 异常
4. 并发/状态竞争
5. 兼容
6. 稳定性/压力

## 正式测试用例格式规范

### 固定字段
标准字段至少包含：
- 用例ID
- 模块
- 用例标题
- 前置条件
- 操作步骤
- 预期结果
- 优先级
- 备注

### 操作步骤与预期结果规则
这是硬约束：
1. 操作步骤与预期结果必须**一一对应**。
2. 编号统一使用 `#`，不要用 `1. 2. 3.`。
3. 即使某一步没有对应内容，也必须保留空的 `#`。

示例：

操作步骤：
- # xxx
- # xxx
- # xxx

预期结果：
- # xxx
- # xxx
- #

### 用例设计注意点
- 不要把多个独立预期混成一大段，导致无法执行回填。
- 不要只写正常流程；复杂项目必须补边界/异常/并发/兼容/稳定性。
- 不要脱离原始资料凭空扩展规则。
- 如果预期基于当前理解而非明确规则，需在备注或澄清项中说明。

## 飞书落地规范
如果目标是飞书表格/文档：

### 表格写入
- 先确认表头和目标 sheet
- 优先小批量写入
- 避免一次性大批量 append
- 每批写入后回读校验
- 如果遇到 `too many request`，改为更小批次并串行处理

### 文档写入
- 优先 append 或局部替换
- overwrite 仅在明确要整篇重写时使用
- 更新完成后回读或根据返回结果确认成功

### 完成标准
不要把“文本生成完成”当成完成。只有以下条件满足才算完成：
- 内容已成功写入目标飞书载体
- 关键内容已回读确认
- 格式符合要求

## 质量检查清单
结束前至少核对：
- 是否完全基于需求文档/PRD/现有资料
- 是否已完成需求拆解
- 是否已完成测试点梳理
- 是否已输出问题澄清项
- 是否存在应补未补的矩阵
- 是否覆盖主流程、边界、异常、并发、兼容、稳定性
- 是否满足用例格式约束
- 如果落地飞书，是否已回读校验

## 常见踩坑
1. 未读资料就直接写用例
2. 没有范围冻结，导致不断发散
3. 没有矩阵，导致关键组合漏测
4. 只提澄清项，不做闭环
5. 步骤与预期不对应，后续无法执行回填
6. 忽略飞书授权/限流
7. 没回读就宣称已完成

## 推荐输出节奏
默认建议按这个顺序与用户对齐：
1. 需求拆解
2. 测试点梳理
3. 问题澄清项
4. 覆盖矩阵（必要时）
5. 正式测试用例
6. 飞书落地
7. 回读确认
8. 补充冒烟 / 回归 / 专项分组

## 何时需要额外提醒用户
以下情况应明确提醒：
- 资料不足，无法可靠生成正式用例
- 关键规则存在歧义，需先澄清
- 飞书写入需要授权
- 飞书接口限流，需要分批处理
- 用户已有固定格式约束，必须先锁定后再写

## 最终目标
把 AI 写测试用例这件事，从“一次性生成文本”升级成“可复用、可落地、可回读校验的正式测试资产生产流程”。
