---
name: comprehensive-test-case-writer
description: |
  全面测试用例编写和优化技能。当用户需要基于需求文档、现有测试用例、评审意见或缺陷单设计、评审、优化、修订测试用例时使用。
  适用于新写测试用例、评审现有用例、按评审意见整改、把分析型初稿收敛为新人可直接执行的正式用例，以及将结果写入飞书 Sheet / 飞书文档。
  技能重点解决：步骤与预期不一一对应、前置条件依赖隐含上下文、使用 A/B/C 等抽象代称、预期结果模糊不可量化、主回归与专项场景混放，以及项目组评审时“语言太技术化、单条用例无法独立理解”的问题。
compatibility:
  tools:
    - read
    - edit
    - write
---

# 全面测试用例编写器

## 适用场景
- 基于需求文档编写测试用例
- 评审并修订现有测试用例
- 根据评审意见做二轮、三轮整改
- 将“分析型初稿”收敛为“新人可直接执行”的正式用例
- 输出到飞书 Sheet / 飞书文档 / 结构化表格
- 输出给产品、运营、业务、研发共同参与的项目组评审

## 核心目标
产出**可交付、可执行、可复核、可复用、可评审**的正式测试用例，而不是停留在分析草稿。

默认交付标准不是“只有测试工程师能看懂”，而是：
- 测试人员可以执行
- 产品/运营/业务可以评审
- 单条用例脱离上下文仍可独立理解

## 默认写作视角
除非用户明确要求“测试执行版 / 技术版 / 自动化导向版”，否则默认按**项目组评审版**输出。

项目组评审版的默认要求：
- 语言直白、场景化，尽量让非测试角色也能读懂
- 少用测试黑话，避免整段内容像内部测试术语堆砌
- 每条用例必须自包含，不依赖前后文补全理解
- 预期结果要写“实际会看到什么”，不是只写抽象判断词

若用户明确要求“测试执行版”，可以保留更强的测试术语和更紧凑的表达，但仍必须满足结构合规与独立执行。

## 固定输出规范
1. 字段顺序默认固定；除非用户明确要求其他模板，不自行改字段。
2. **步骤描述**与**预期结果**必须严格一一对应，条数必须一致。
3. 步骤描述统一用 `#` 前缀，禁止数字序号。
4. 预期结果统一用 `#` 前缀。
5. 某条步骤没有实际操作内容时，写 `#/`。
6. 某条预期没有实际结果内容时，写 `#/`。
7. 若 1 步对应多条结果，必须补齐步骤侧占位 `#/`；若步骤多于结果，结果侧补 `#/`。
8. 前置条件无内容时写 `/`，不要留空。
9. 正式用例默认不用 A/B/C、页面1/页面2、成员1/成员2 这类抽象代称；优先写真实页面名、真实角色名、真实对象名。
10. 无法确认的规则不要写死，使用“按产品定义”并在备注中标出待确认点。
11. 一条用例默认只承载一个核心验证目标；不要把多个场景、多个目标、多个异常链路混在同一条。
12. 每条用例必须满足“单独拿出来也能评审、也能执行”。

## 项目组评审版额外硬规则
1. 少用“校验 / 验证 / 执行 / 检查 / 触发”这类测试黑话；能改成直白动作就改。
2. 多用真实场景表达，如“进入页面后看到什么”“点击哪里后页面怎么变化”“切换后显示什么内容”。
3. 前置条件必须写清对象、页面、数据、状态、环境；禁止只写“已进入页面”“已有数据”这类空泛口径。
4. 步骤必须写清：
   - 在哪个页面/区域操作
   - 对哪个对象操作
   - 做了什么动作
5. 预期结果必须写清：
   - 页面上具体显示什么
   - 哪个对象状态发生了什么变化
   - 用户能直接看到或业务能直接理解的结果是什么
6. 禁止大量使用“显示正确”“切换成功”“结果正常”“符合预期”“无异常”这类抽象结论词作为主要预期。
7. 单条用例必须能独立回答 4 个问题：
   - 在什么场景下测
   - 对哪个对象操作
   - 具体怎么操作
   - 操作后应该看到什么

## 工作流程
1. **输入识别**
   - 判断输入是需求文档、现有用例、评审意见、缺陷单，还是飞书表格/文档链接。
   - 判断任务属于：新写、评审、修订、补覆盖、落表交付。
   - 判断交付对象是测试内部使用，还是项目组评审使用；若用户未说明，默认按项目组评审版处理。

2. **范围澄清**
   - 明确模块范围、版本范围、目标表头、输出位置、是否区分主回归集 / 专项验证集。
   - 若页面定义、成员对象、时间口径、网络条件、日志条件缺失，先补齐再继续。
   - 若用例准备给项目组评审，优先补清页面名、对象名、用户动作、界面结果，避免依赖测试上下文。

3. **内容分析**
   - 提取功能点、业务规则、输入输出、页面流转、状态切换、异常分支、恢复路径、兼容点。
   - 识别高风险项：时间调度、弱网、升级兼容、竞争态、缓存、恢复、并发、稳定性。
   - 判断每个候选用例是否能单独讲清一个场景；若不能，先拆再写。

4. **初稿生成 / 问题识别**
   - 新写用例：先生成结构化初稿。
   - 评审现有用例：重点检查是否单一场景、是否独立执行、是否存在隐含上下文、抽象代称、模糊预期、步骤/预期不对齐。
   - 额外检查是否存在“测试人能懂、项目组看不懂”的术语密度过高问题。

5. **修订与收敛**
   - 把分析型内容修成正式可执行用例。
   - 目标标准：
     - 新人可直接执行
     - 项目组可直接评审
     - 步骤与预期逐条对应
     - 前置条件工程化
     - 单条用例自包含
     - 适合后续自动化迁移

6. **落表 / 落文档**
   - 写入飞书前，先确认字段顺序、模块归类、子表/文档位置、覆盖范围。
   - 大批量写入后必须回读关键区间校验，避免截断、串行污染、尾部丢失。

7. **最终复核**
   - 逐条检查：
     - 步骤数 = 预期数
     - 缺项已补 `#/`
     - 前置条件无隐含上下文
     - 无未经映射的抽象代称
     - 预期尽量可验证、可量化
     - 非测试角色是否也能大致看懂
     - 单条拿出来是否仍可独立评审
   - 再确认输出已达到“可交付、可执行、可复用、可评审”。

## 质量检查清单
- 是否单一场景，避免一条用例混多个目标
- 是否可以脱离上文独立执行
- 是否可以脱离上文独立评审
- 前置条件是否写清环境、状态、对象、网络、时间口径
- 前置条件是否仍存在“已进入页面 / 已有数据 / 已完成配置”这类空泛写法
- 步骤与预期是否逐条对应
- 步骤是否写清页面、对象、动作
- 预期是否写清具体页面表现、数据变化、状态变化
- 是否还存在“正常/正确/符合需求/及时/成功/无异常”这类模糊口径
- 是否仍有过多“校验/验证/执行/检查”等测试黑话
- 是否把高成本场景与主回归场景区分开
- 若写入飞书，是否回读校验头部、关键区间、尾部

## 常见错误与纠正
- **只修内容，不修结构**：内容明确不等于格式合规；必须额外校验步骤/预期条数。
- **1步对应多预期**：补步骤侧 `#/`，不要偷懒。
- **多步少预期**：补预期侧 `#/`。
- **前置条件借用上下文**：把依赖条件写回当前用例。
- **抽象代称未落真实对象**：改成真实页面名、角色名、设备名、对象名。
- **术语过重，只有测试看得懂**：改写成项目组评审能直接理解的场景化表述。
- **预期只写抽象判断**：改成“用户实际看到什么 / 页面实际变成什么 / 数据实际如何变化”。
- **单条用例离开上下文就看不懂**：补足场景、对象、入口、动作、结果，必要时拆分用例。
- **批量写飞书后未校验**：至少回读头部、用户点名区间、尾部。
- **写入被截断仍误报完成**：以实际回读结果为准，不以“调用成功”替代“落地成功”。

## 默认输出建议
若用户未指定模板，优先输出结构化表格，至少包含：
- ID
- 一级模块
- 二级模块（如需要）
- 用例名称
- 优先级
- 用例类型（如需要）
- 前置条件
- 步骤描述
- 预期结果
- 备注（如需要）

## 自检口令（内部使用）
在交付前，默认自问以下问题：
- 这条用例如果给产品经理单独看，对方能不能看懂？
- 这条用例如果脱离上一条、下一条，还能不能独立成立？
- 步骤有没有省页面、省对象、省动作？
- 预期有没有只写“正确/正常/成功”而没写具体表现？
- 这条用例是不是混了两个以上验证目标？

---

本技能只负责把测试用例做对、做实、做成可执行交付物；不把未确认项写成既定需求。
