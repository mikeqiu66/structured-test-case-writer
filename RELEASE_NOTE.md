# RELEASE_NOTE

## 当前版本规则

- 正式版本 tag 统一采用整数版本体系：`V4`、`V5`、`V6`、`V7` ...
- 新版本 tag 必须打在当前主线最新提交上
- 新版本提交必须包含前一版本内容，保证版本链连续
- 当前仓库已整理的版本线为：`V4`、`V5`、`V6`
- 后续默认从 `V7`、`V8` 继续递增

## V5

发布日期：2026-05-15  
仓库：`structured-test-case-writer`  
Tag：`V5`  
Commit：`65e115e`

### 本版定位
V5 不是简单补文案，而是把这份 skill 从“会写测试用例”升级成“能生成、评审、修订、落表，并能约束输出质量的正式测试用例技能”。

### 相比上一版的核心变化

#### 1. 强化了 skill 顶部描述
新增并明确了以下内容：
- 支持能力
- 典型触发场景
- 更贴近实际工作的 skill 说明风格

使 skill 不再只是泛化描述，而是更像一个可触发、可复用、可维护的正式能力模块。

#### 2. 补充了更具体的支持能力定义
V5 明确支持：
- 根据需求文档、现有用例、评审意见生成或修正测试用例
- 按模块、场景、优先级、测试类型输出结构化测试用例
- 对现有测试用例执行多轮评审并沉淀整改意见
- 将“分析型初稿”收敛为“新人可直接执行”的正式测试用例
- 支持主回归集 / 专项验证集分层整理

#### 3. 补充了典型触发场景
V5 增加了更明确的触发语义，覆盖：
- 写测试用例
- 补测试用例
- 评审测试用例
- 优化测试用例
- 根据评审意见修正用例
- 把用例改成新人可直接执行
- 输出到飞书 Sheet / 飞书文档

这使 skill 更容易被正确触发，也便于后续扩展自动选择逻辑。

#### 4. 新增正式工作流程
V5 新增了一套从输入到交付的标准流程：
1. 输入识别
2. 范围澄清
3. 内容分析
4. 初稿生成 / 问题识别
5. 修订与收敛
6. 落表 / 落文档
7. 最终复核

这部分是本次升级最关键的变化之一，它把 skill 从“描述能力”推进到“定义执行流程”。

#### 5. 基于实战补充了常见错误与纠偏规则
V5 不再只保留通用测试用例建议，而是把这次实际写用例、评审用例、修订用例过程中暴露的问题沉淀成规则，新增包括：
- 前置条件依赖隐含上下文
- 使用 A/B/C/D 等抽象代称但未建立映射
- 预期结果可理解但不可量化
- 前置条件不够工程化
- 主回归与专项场景混放

这部分直接提升了 skill 的实用性和稳定性。

#### 6. 强化“新人可直接执行”标准
V5 明确把测试用例质量标准收紧到：
- 单条用例可独立执行
- 前置条件具备工程化描述
- 步骤与预期结果严格一一对应
- 页面、成员、时间、网络、日志、缓存、版本等关键上下文必须显式写明
- 避免依赖口头解释或执行经验

#### 7. 强化量化口径意识
V5 特别强调：
- 不要只写“正常”“正确”“及时”“符合需求”
- 应尽量补充时间窗口、误差阈值、日志tag、任务数量、响应时长、轮次数等量化标准

这为后续自动化迁移和多人协同评审打下基础。

### 本版收益
通过 V5，这份 skill 更适合用于：
- 固件测试用例编写
- 测试用例评审与整改
- 飞书表格/文档交付
- 多轮评审闭环
- 新人可执行版本沉淀
- 主回归 / 专项集分层整理

### 兼容性说明
- 未改变原有字段模板顺序
- 未改变步骤与预期的固定输出规范
- 保持与原有技能主体结构兼容
- 本次主要为说明层、流程层、质量约束层增强

### 建议下一步
建议在后续版本补充：
- Definition of Done（最终交付标准）
- 边界/禁忌说明（什么情况下必须先追问）
- 飞书 Sheet / 文档落表时的检查清单
